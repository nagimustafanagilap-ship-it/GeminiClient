name: Build Gemini Client
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Build with Gradle
        run: |
          if [ -f "./GeminiClient.zip" ]; then
            unzip GeminiClient.zip -d .
            rm GeminiClient.zip
          fi
          chmod +x gradlew
          ./gradlew build
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GeminiClient-Jar
          path: build/libs/*.jar

